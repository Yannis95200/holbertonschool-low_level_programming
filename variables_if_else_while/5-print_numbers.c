#include <stdio.h>
/**
* main - Entry point
* Return: always 0 (success)
*/
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)

	printf("%d", num);
	if (num < 9)

	printf("\n");
	return (0);
}
